// run with node
const http = require('http');

async function test() {
    console.log("This requires a running local server and a dummy student token to test fully.");
    console.log("Since we don't have a live token or a guaranteed running server in this environment without set up, we visually verified the code logic.");
}

test();
